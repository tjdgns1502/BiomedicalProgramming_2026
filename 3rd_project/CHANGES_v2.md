# CHANGES_v2 — v1(커밋 3086eb9) 대비 수정 내역

v1 파일(`R/08_figures.R`, `report.Rmd`, `report.html`, `figs/*.png`)은 그대로 보존하고, 수정본은 새 파일로 만들었다.

| v1 | v2 |
|---|---|
| `R/08_figures.R` | `R/08_figures_v2.R` |
| `figs/*.png` | `figs/v2/*.png` (11장 전부 재생성) |
| `results/08_figures.rds` | `results/08_figures_v2.rds` |
| `report.Rmd`, `report.html` | `report_v2.Rmd`, `report_v2.html` |

데이터·시드·시뮬레이션 결과는 v1과 같다. `results/0*.rds`는 바뀌지 않았다. 바뀐 것은 그림의 표현 3건과 보고서의 서술 수정 3건이다.

## 그림 결함 (발견 경로: 커밋 후 11장을 전부 직접 열어 확인)

| 그림 | v1의 문제 | v2 |
|---|---|---|
| `fig1_qq_groupwise` | `facet_grid(group ~ panel)`가 4×3 격자를 만들어 12칸 중 6칸이 비어 있고 왼쪽 라벨이 잘림 | `facet_wrap(~lab, ncol = 2)`로 (자료, 군)마다 패널 1개, 6칸 모두 사용. 여성 중성지방 패널이 4,233 mg/dL 관측치 하나에 눌리는 점을 캡션에 명시 |
| `fig2a_resampling` | 그림은 부트스트랩이 왼쪽인데 캡션과 보고서 본문은 "왼쪽=순열, 오른쪽=부트스트랩" | 패널 순서를 명시(순열이 왼쪽)하고, 캡션도 LEFT/RIGHT 대신 패널 이름으로 서술 |
| `fig5_rejection_vs_n` | "Fagerland's published value" 라벨이 곡선과 겹침 | 라벨을 곡선 위로 이동 |

## 보고서 서술 수정 (발견 경로: 프로세스 문서를 쓰며 표 4·5의 칸별 차이를 다시 계산)

민감도 B(평균·왜도·SD비 동일, 중앙값 이동)와 논문 표 4·5의 칸별 절대 차이를 다시 계산했다(%p).

| | n=10 | n=25 | n=50 | n≥100 |
|---|---:|---:|---:|---:|
| 표 4 (Welch·WMW, 감마·로그정규 중 최대) | 1.43 | 0.31 | 0.11 | 0.17 이하 |
| 표 5 (감마 / 로그정규) | 3.5 / 4.4 | 0.6 / 0.9 | 0.1 / 0.4 | 0.1 이하 |

| # | v1 서술 | 문제 | v2 서술 |
|---|---|---|---|
| 1 | "표 4·5의 **n ≥ 25 모든 칸에서 0.3%p 이내**", "표 2·4·5가 **전부 몬테카를로 오차 안에서 재현**" | 표 5의 n=25는 0.6–0.9%p, n=10은 3.5–4.4%p 벗어남. 표 4의 n=25도 0.31%p | 표 4는 n≥25에서 0.31%p 이내, 표 5는 n≥50에서 0.4%p 이내. n≤25는 WMW 근사 방식 차이로 벗어남을 명시 |
| 2 | "논문은 `wilcox.test()`의 **정확검정** 자동 전환을 그대로 썼다", "규칙 4가 옳다는 증거" | `04b`에서 연속성 보정 근사·R 기본값·정확검정이 64칸 모두 칸별로 완전히 같아(최대 차이 0.000%p) n=10에서는 둘을 구분할 수 없음. "규칙 4가 옳다"는 이 데이터가 보이는 사실이 아니라 의견 | "논문의 n=10 값은 R 기본 설정과 일치한다. 정확검정과 연속성 보정은 구분되지 않는다." 규칙 4 문장은 "결정 사항"으로 바꿈 |
| 3 | 표 2 차이가 "**반올림 오차 안**" | 최대 차이 감마 0.006, 로그정규 0.012가 ±0.005를 넘음 | "논문 값 가까이, 반올림 수준에 가깝다" |

수치 자체(캐시된 `.rds`)는 바뀌지 않았다. 바뀐 것은 그 수치를 요약하는 문장이다.

## 알려진 미해결 사항 (v2에서도 유지)

- 논문의 칸별 전체 표(Additional file 1)에 접근하지 못해 448칸 전체 대조는 하지 못했다.
- 비어 있는 `results/`에서 스크립트 12개를 처음부터 다시 실행해 본 적이 없다.
- 규칙 4(`correct=FALSE`)를 유지할지는 결정이 필요하다. 유지하면 표 4 평균 기준 n=10에서 +1.0–1.5%p, n=25에서 약 +0.3%p가 논문과 남는다.

자세한 내용은 [docs/work_process_and_issues.md](docs/work_process_and_issues.md)에 있다.
